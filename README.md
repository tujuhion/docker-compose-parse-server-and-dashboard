# Docker Compose Parse Server, Parse Dashboard and mongodb with GraphQl enable

## Installation
```sh
$ wget https://raw.githubusercontent.com/tujuhion/docker-compose-parse-server-and-dashboard/master/docker-compose.yml
$ docker-compose up -d
```
